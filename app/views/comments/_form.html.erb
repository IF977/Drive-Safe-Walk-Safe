
<%= form_for([@acidente, @acidente.comments.build]) do |f| %>
  <p>
    <%= f.label :autor %><br>
    <%= f.text_field :autor %>
  </p>
  <p>
    <%= f.label :Comenário %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>